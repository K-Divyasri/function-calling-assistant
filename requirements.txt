# Core: NOTHING. The offline assistant runs on the Python standard library alone.
# (do_math, web_search, and query_database are pure stdlib; the fake model is regex.)

# --- Optional upgrades (uncomment to enable) ---

# Real function-calling through a live LLM (the --real flag):
# litellm>=1.40
# python-dotenv>=1.0

# Live, keyless weather via Open-Meteo (the --live-weather flag):
# requests>=2.28

# The Streamlit web app (app.py):
# streamlit>=1.30
# requests>=2.28
